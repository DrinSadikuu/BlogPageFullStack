{
  "name": "BlogPage",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "start": "node api/index.js"  
  },
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
